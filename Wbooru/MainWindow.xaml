<Window x:Class="Wbooru.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Wbooru"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:Converter="clr-namespace:Wbooru.ValueConverters"
        xmlns:local1="clr-namespace:Wbooru.UI.Panels"
        mc:Ignorable="d"
        Title="Wbooru" Height="450" Width="800">
    <Window.Resources>
        <Converter:AutoFitHeightConverter x:Key="AutoFitHeightConverter"/>
        <Converter:ItemMarginConverter x:Key="ItemMarginConverter"/>
        <sys:UInt32 x:Key="GridItemWidth">150</sys:UInt32>
        <sys:UInt32 x:Key="GridItemMarginWidth">10</sys:UInt32>
    </Window.Resources>
    <Grid DataContext="{Binding Source={StaticResource SampleItems}}">
        <ScrollViewer x:Name="MainPanel">
            <ListBox
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            ItemsSource="{Binding BooruItems}"
            x:Name="LB">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="{x:Type ListBoxItem}">
                        <Setter Property="RenderTransform">
                            <Setter.Value>
                                <TranslateTransform/>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.400"/>
                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(TranslateTransform.Y)" From="200" To="0" Duration="0:0:0.500">
                                            <DoubleAnimation.EasingFunction>
                                                <CircleEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Style.Triggers>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <local1:GridFlowPanel 
                            Width="{Binding ActualWidth,ElementName=LB}"
                                GridItemWidth="{Binding Source={StaticResource GridItemWidth}}"
                                GridItemMarginWidth="{Binding Source={StaticResource GridItemMarginWidth}}" 
                                />
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.Template>
                    <ControlTemplate>
                        <ItemsPresenter></ItemsPresenter>
                    </ControlTemplate>
                </ListBox.Template>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel 
                                Width="{Binding Source={StaticResource GridItemWidth}}"
                                Margin="{Binding Converter={StaticResource ItemMarginConverter},Source={StaticResource GridItemMarginWidth}}">

                            <Image Width="{Binding Source={StaticResource GridItemWidth}}" Source="{Binding GrallyItem.ImageSource}">
                                <Image.Height>
                                    <MultiBinding Converter="{StaticResource AutoFitHeightConverter}">
                                        <Binding RelativeSource="{RelativeSource Self}" Path="Source"/>
                                        <Binding Source="{StaticResource GridItemWidth}"/>
                                    </MultiBinding>
                                </Image.Height>
                            </Image>

                            <CheckBox IsChecked="{Binding GrallyItem.IsVoted}" FlowDirection="RightToLeft" Content="{Binding GrallyItem.VotedCount}">
                                <!--Heart Style todo-->
                            </CheckBox>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </ScrollViewer>
    </Grid>
</Window>
